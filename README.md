# Independent App Migration (Self-Hosted)

This application has been migrated from Base44 to be fully self-hosted, including the database (local.db) and API (server.js).

## Setup & Running Locally

1. Install dependencies (Node.js required):
   ```bash
   npm install